# Project Agent — Agent Teacher (design-os-pedagogy)

- **Product:** Agent Teacher: pedagogy knowledge library, local teacher rehearsal, draft publication pipeline.
- **Repo:** github.com/jangtrinh/design-os-pedagogy (public). Pages: https://jangtrinh.github.io/design-os-pedagogy/ (source `main:/docs`, Jekyll).
- **Stack:** Python 3.10+, stdlib tests; `requirements.txt` only for registry/publication.

## localhost_port

- `localhost_port: 4322`
- Rehearsal: `python3 run_rehearsal.py --port 4322` (binds loopback). Registry: `~/.config/project-localhost-ports.json`.
